{
  "name": "awesome-agile-resources",
  "version": "1.0.0",
  "private": true,
  "description": "Community-curated Agile resources with generated README and JSON export.",
  "scripts": {
    "validate:resources": "node scripts/validate-resources.js",
    "validate:schema": "node scripts/validate-schema.js",
    "generate:readme": "node scripts/generate-readme.js",
    "generate:json": "node scripts/generate-json.js",
    "generate:all": "node scripts/validate-resources.js && node scripts/generate-readme.js && node scripts/generate-json.js && node scripts/validate-schema.js",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
